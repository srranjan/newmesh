- name: Handle Default
  hosts: localhost
  gather_facts: false
  connection: local
  inventory:
    hosts:
      localhost:
        ansible_connection: local
  vars:
    ansible_connection: local
  tasks:
    - name: Trace Step
      debug:
        msg: "I am the Default Handler, I take what nobody wishes to take!!"
        verbosity: 1

