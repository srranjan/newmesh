---
- name: Test Custom Plugin
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Call custom HTTP module
      my_custom_http:
        url: "https://api.example.com/alert"
        method: POST
        body:
          message: "Hello from custom plugin"

